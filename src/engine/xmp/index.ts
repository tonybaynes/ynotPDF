/** XMP: read what a packet says, patch it without disturbing anything else (M72, ADR 0017). */

export { readXmp, patchXmp } from './xmp';
export { XMP_NS, type XmpFacts, type XmpReading } from './types';
