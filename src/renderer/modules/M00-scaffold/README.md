# M00 — Scaffold

Application-level commands that exist before any feature module: `file.open`,
`file.openRecent`, `file.close`, `edit.undo`, `edit.redo`, `app.commandPalette`, `app.about`,
`app.quit`, `dev.toggleDevTools`. Spec: `docs/modules/M00-scaffold.md`.
