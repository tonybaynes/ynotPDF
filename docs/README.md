# docs

- `adr/` — architecture decision records, numbered `NNNN-slug.md`. A contract change
  (`PdfEngine`, `Document`, `Command`, `ModuleManifest`, IPC) needs one merged first.
- `modules/` — one brief per module; the "Your task" section is the conversation prompt.
